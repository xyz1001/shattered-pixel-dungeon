# Fork Mod 开发规范

## 使用范围

- 本 Fork 仅供个人使用。实现 mod 功能时优先保持简单直接，无需为不可信输入、
  多用户隔离、兼容未知第三方扩展等场景加入过度的防御性逻辑。
- 这不免除基本正确性要求：不得引入明显崩溃、数据损坏或破坏正常游戏流程的行为。

## 仓库模型

- 本仓库是 Shattered Pixel Dungeon 的 Fork。
- 仅在用户明确要求“拉取原仓库最新提交并 rebase”时，才可更新 `upstream`
  并将本地分支 rebase 到 `upstream/master`；禁止将上游历史 merge 到 mod 分支。
- 其他场景仅以本仓库已有提交为基准，不访问、不更新 `upstream`，也不进行上游
  比较或 rebase。
- 将上游源码视为外部维护代码，优先新增文件而不是修改既有文件。

## 开发前确认

- 每次修改前，先向用户说明实施方案和范围评估：目标、涉及文件、预期改动、
  提交或历史影响、验证方式及主要风险。
- 只有收到用户明确确认后，才能修改文件、变更提交历史、更新配置或执行会产生
  构建产物的命令；只读检查不受此限制。

## Mod 边界

- 每个提交只实现一个面向用户的 mod 功能。mod 提交不得混入无关功能、
  重构、纯格式调整或 CI 修改。
- 功能实现放在
  `core/src/main/java/com/shatteredpixel/shatteredpixeldungeon/mod/` 下，
  实际可行时使用功能专属子包。
- 每项功能必须拥有其实现文件；避免修改由其他功能拥有的文件。
- 每个面向用户的 mod 功能都必须注册 Mod 菜单开关，支持独立启用或关闭，且默认关闭。
- 设置、UI 注册、生命周期分发等共享代码应保持最少。除非依赖明确且必要，
  一个功能不得依赖另一个功能。
- 优先采用数据驱动注册或通用扩展点，使新增 mod 无需修改共享注册表、hook
  分发器或选项 UI。

## 上游接入

- 允许对上游文件作小范围、精确的修改，但仅用于建立通用接入点。改动应隔离、
  有说明，并紧邻对应的既有行为。
- 不得在 `Hero`、`Item`、`Gold`、`MissileWeapon` 等上游类中加入特定功能的
  mod 逻辑。这些类可以调用通用 mod hook，具体实现必须位于 mod 包内。
- 尽量减少上游文件的接入点数量；已有通用 hook 的语义适用时，必须复用它，
  不要新增 hook。
- 禁止大范围重构、重新格式化、无关清理或 import 整理；这些改动会增加
  rebase 冲突。
- 若某项用户需求必须大范围修改上游原始代码，或无法通过少量通用接入点实现，
  可以拒绝该需求，并说明其会显著增加后续 rebase 的冲突与维护成本。

## 历史策略

- 分支历史允许重写。调整既有功能时，amend 或 rebase-edit 该功能的原始提交，
  不要追加修复提交。
- 重写后仍须保持一个功能一个提交。通用 mod 基础设施、CI 与仓库配置必须
  使用独立提交。
- 重写历史前，检查本仓库的提交范围，并确认每个提交仍能独立工作。
- 每次修改完成并通过必要验证后，必须立即创建对应提交，或 amend / rebase-edit
  合入既有的规范功能提交。
- 验证失败时，修复后重新验证；验证通过后立即 amend / rebase-edit 合入对应的
  规范提交，不得追加修复提交。

## 验证

- 每次修改完成后、创建或合入提交前运行范围最小且相关的 Gradle 构建或测试。
- 用户未明确指定构建目标时，修改完成后应询问其要构建 desktop JAR 还是 armv8 APK；
  未获选择前不得主动生成这两类发布产物。
- 构建 Android APK 时，必须使用与 CI 一致的 `./gradlew android:assembleRelease`；不得
  使用 `assembleDebug` 作为交付 APK。该产物为未签名 release APK。
- rebase 上游变更前，只审查重叠的上游文件 hunk，并确认每个通用 hook 仍在
  正确的游戏生命周期位置执行。
- 除非任务明确要求，否则不得修改生成产物。

- 仅在用户明确要求时才执行 rebase；更新 `upstream` 并进行上游 rebase 时，
  还必须明确要求“拉取原仓库最新提交并 rebase”。
